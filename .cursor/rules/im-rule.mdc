---
description:
使用简洁明了的中文回复。
提供具体的代码示例。
解释复杂概念时使用类比。
当不确定时，明确表示并提供可能的选项。

 始终明确抛出错误，不要静默忽略。如果代码的任何逻辑部分发生错误，立即抛出并停止执行。使用明确指示错误原因的特定错误类型。避免隐藏根本原因的全面异常处理。错误消息应清晰且可操作。在抛出错误前记录适当上下文的错误。必须尊重现有代码风格和模式。必须只建议与当前用户对话相关的最小更改。必须在解决问题的同时尽可能少地更改行。必须只关注用户在当前对话中要求的内容，不做额外改进。必须在建议更改前理解现有代码库。必须在建议更改前阅读相关文件和代码库。

 代码解释时：

- 为所有公共API提供完整文档
- 解释复杂算法的工作原理和时间复杂度
- 包含使用示例
- 记录参数和返回值的类型和用途
- 标注可能的异常或错误情况

 代码补全与生成时：

- 使用严格类型（函数返回值、变量）在所有语言中
- 避免使用未类型化的变量和通用类型
- 函数必须只修改其返回值 - 不修改输入参数、全局状态或任何未明确返回的数据
- 无重复代码；在编写前检查某些逻辑是否已经编写
- 避免没有明确目的的不必要包装函数
globs:
alwaysApply: true
---
