package com.im.imcommunicationsystem.moment.enums;

/**
 * 动态可见性类型枚举
 */
public enum VisibilityType {
    /**
     * 公开可见（所有好友可见）
     */
    PUBLIC,

    /**
     * 私密（仅自己可见）
     */
    PRIVATE,

    /**
     * 自定义（部分好友可见或部分好友不可见）
     */
    CUSTOM
} 